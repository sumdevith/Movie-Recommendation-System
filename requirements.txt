# =============================================================================
# requirements.txt — Distributed NCF MovieLens Recommender
# =============================================================================
# Core ML
torch>=2.1.0
torchvision>=0.16.0          # pulls in compatible torch version

# Distributed data processing
pyspark>=3.4.0               # TorchDistributor requires Spark >= 3.4
pyarrow>=12.0.0              # required for Spark ↔ pandas interchange (pandas_udf)

# Scientific Python
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0          # optional: used in notebooks for quick baselines

# Utilities
tqdm>=4.66.0                 # progress bars (optional, used in notebooks)
matplotlib>=3.7.0            # loss / RMSE plots (optional)
